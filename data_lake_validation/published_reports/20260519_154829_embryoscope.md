# Embryoscope Data Lake Validation Report
**Timestamp**: 2026-05-19 15:48:29  
**System**: Embryoscope  
**Environment**: Production AWS (Athena) vs Local DuckDB (`huntington_data_lake.duckdb`)

---

## 1. Schema Comparison
* **patients**: Local has 11 columns, Athena has 9.
* **treatments**: Local has 7 columns, Athena has 6.
* **embryo_data**: Local has 173 columns, Athena has 171.
* *Note: Data fields align semantically; casing maps from local `PascalCase`/`camelCase` to Athena `snake_case`.*

### 1.1 Column Differences by Source (Excluding Case-Only Differences)

#### Table: patients
* **Only in Local**: 
  - `prontuario` (Patient prontuario matching key)
  - Local metadata fields: `_extraction_timestamp`, `_location`, `_row_hash`, `_run_id`
* **Only in Athena**: 
  - `patient_sk` (Surrogate key)
  - AWS dlt metadata fields: `_dlt_id`, `bronze_updated_at`

#### Table: treatments
* **Only in Local**:
  - Local metadata fields: `_extraction_timestamp`, `_location`, `_row_hash`, `_run_id`
* **Only in Athena**:
  - `treatment_sk` (Surrogate key)
  - AWS dlt metadata fields: `_dlt_id`, `bronze_updated_at`

#### Table: embryo_data
* **Only in Local**:
  - `embryo_number` (Locally engineered sequential feature)
  - Local metadata fields: `_extraction_timestamp`, `_location`, `_row_hash`, `_run_id`
* **Only in Athena**:
  - `embryo_data_sk` (Surrogate key)
  - AWS dlt metadata fields: `_dlt_id`, `bronze_updated_at`

---

## 2. Row Count Comparison
| Table | Local Count | Athena Count | Difference | Pct Diff |
| :--- | :--- | :--- | :--- | :--- |
| **patients** | 13,483 | 12,877 | -606 | -4.49% |
| **treatments** | 22,927 | 21,878 | -1,049 | -4.58% |
| **embryo_data** | 121,158 | 139,792 | +18,634 | +15.38% |

---

## 3. Clinic Breakdown (Row Counts)

### Patients
| Clinic / Location | Local Count | Athena Count | Difference |
| :--- | :--- | :--- | :--- |
| **Belo Horizonte** | 2,516 | 2,520 | +4 |
| **Brasilia** | 1,770 | 1,788 | +18 |
| **Vila Mariana** | 3,205 | 3,214 | +9 |
| **Ibirapuera** | 5,992 | 5,355 | -637 |

### Treatments
| Clinic / Location | Local Count | Athena Count | Difference |
| :--- | :--- | :--- | :--- |
| **Belo Horizonte** | 3,322 | 3,327 | +5 |
| **Brasilia** | 2,423 | 2,472 | +49 |
| **Vila Mariana** | 4,257 | 4,273 | +16 |
| **Ibirapuera** | 12,925 | 11,806 | -1,119 |

### Embryo Data
| Clinic / Location | Local Count | Athena Count | Difference |
| :--- | :--- | :--- | :--- |
| **Belo Horizonte** | 19,112 | 26,255 | +7,143 |
| **Brasilia** | 15,062 | 19,042 | +3,980 |
| **Vila Mariana** | 32,036 | 37,311 | +5,275 |
| **Ibirapuera** | 54,948 | 57,184 | +2,236 |

---

## 4. Key Mismatches & Discrepancies

### Patients
* **Unique Keys in Local but missing in Athena Silver**: 656
* **Unique Keys in Athena but missing in Local**: 50
* **Traceable PatientIDx examples (Local Only)**:
  * `PC1P7BHG_45703.4767686343` (Prontuario `851422` - Ibirapuera)
  * `PC1P7BHG_45704.3770025000` (Prontuario `840502` - Ibirapuera)
* **Traceable PatientIDx examples (Athena Only)**:
  * `NEXTGEN_43895.6049278935` (Prontuario `overnight test` - Brasilia)
  * `S4JP1389_44587.4418880787` (Prontuario `testslide8` - Brasilia)

### Treatments (Composite key: `PatientIDx || TreatmentName`)
* **Unique Treatments in Local but missing in Athena**: 1,161
* **Unique Treatments in Athena but missing in Local**: 112
* **Traceable composite examples (Local Only)**:
  * `NEXTGEN_43052.4993192940 || 2025-2153` (Ibirapuera)
  * `PC1P7BHG_44651.7014901736 || 2025-472` (Ibirapuera)

### Embryo Data
* **Unique EmbryoIDs in Local but missing in Athena**: 6,444
* **Unique EmbryoIDs in Athena but missing in Local**: 25,078
* **Traceable EmbryoID examples (Local Only)**:
  * `D2025.05.15_S04096_I3027_P-1` (Patient `PC1P7BHG_45071.6284492593`, Treatment `2025-970` - Ibirapuera)
* **Traceable EmbryoID examples (Athena Only)**:
  * `D2024.03.27_S02329_I3254_P-1` (Patient `PC1PPEMH_45378.3748052315`, Treatment `27/03/2024` - Belo Horizonte)

---

## 5. Root Cause Analysis
1. **Ibirapuera Ingestion Failure**: The patient/treatment deficit in Athena is concentrated in the Ibirapuera clinic. Tracing sample missing keys to the Athena Bronze layer returned 0 records, confirming that the files were never extracted or uploaded to AWS S3 from that clinic server.
2. **Empty Embryo Annotations Filtering**: Athena has significantly more embryos because the local consolidation script filters out embryos where all annotation/morphokinetic columns are `NULL`, whereas the Athena pipeline currently ingests and stores them.
3. **Administrative Records**: The "Athena Only" patients are test or administrative entries (e.g. `overnight test`, `testslide8`) that the local script filters out.

---

## 6. Athena Transformation and Filtering Logic SQL

To reconcile the Athena staging layer with the local data lake's business transformations, execute the following view DDL queries in Athena:

### Patients View DDL (Replicates Numeric Cleaning & Deduplication)
```sql
CREATE OR REPLACE VIEW silver_embryoscope_staging.patients_filtered AS
WITH deduplicated_patients AS (
    SELECT 
        raw_json,
        _extraction_timestamp,
        _location,
        _run_id,
        _row_hash,
        json_extract_scalar(raw_json, '$.PatientIDx') as patient_id_x,
        json_extract_scalar(raw_json, '$.PatientID') as patient_id,
        json_extract_scalar(raw_json, '$.FirstName') as first_name,
        json_extract_scalar(raw_json, '$.LastName') as last_name,
        json_extract_scalar(raw_json, '$.DateOfBirth') as date_of_birth,
        ROW_NUMBER() OVER (
            PARTITION BY json_extract_scalar(raw_json, '$.PatientIDx') 
            ORDER BY _extraction_timestamp DESC
        ) as rn
    FROM bronze_embryoscope_staging.patients
)
SELECT 
    patient_id_x,
    CAST(REPLACE(patient_id, '.', '') AS BIGINT) as patient_id,
    first_name,
    last_name,
    date_of_birth,
    _extraction_timestamp,
    _location as source_server,
    _run_id,
    _row_hash
FROM deduplicated_patients
WHERE rn = 1
  AND patient_id IS NOT NULL 
  -- Remove '.' and verify only digits exist in the cleaned patient_id
  AND regexp_like(REPLACE(patient_id, '.', ''), '^\d+$')
  -- Cast and ensure it's not a zero record
  AND CAST(REPLACE(patient_id, '.', '') AS BIGINT) > 0
  -- Exclude common test/administrative records
  AND LOWER(first_name) NOT LIKE 'overnight%'
  AND LOWER(patient_id) NOT LIKE '%test%';
```

### Treatments View DDL (Replicates Cascading Deletes)
```sql
CREATE OR REPLACE VIEW silver_embryoscope_staging.treatments_filtered AS
WITH deduplicated_treatments AS (
    SELECT 
        _extraction_timestamp,
        _location,
        _run_id,
        _row_hash,
        json_extract_scalar(raw_json, '$.PatientIDx') as patient_id_x,
        json_extract_scalar(raw_json, '$.TreatmentName') as treatment_name,
        ROW_NUMBER() OVER (
            PARTITION BY json_extract_scalar(raw_json, '$.PatientIDx'), json_extract_scalar(raw_json, '$.TreatmentName') 
            ORDER BY _extraction_timestamp DESC
        ) as rn
    FROM bronze_embryoscope_staging.treatments
)
SELECT 
    patient_id_x,
    treatment_name,
    _extraction_timestamp,
    _location as source_server,
    _run_id,
    _row_hash
FROM deduplicated_treatments
WHERE rn = 1
  -- Cascading Filter: Only keep treatments associated with valid patients
  AND patient_id_x IN (SELECT patient_id_x FROM silver_embryoscope_staging.patients_filtered);
```

### Embryo Data View DDL (Replicates Empty Annotations Filtering)
```sql
CREATE OR REPLACE VIEW silver_embryoscope_staging.embryo_data_filtered AS
WITH deduplicated_embryos AS (
    SELECT 
        _extraction_timestamp,
        _location,
        _run_id,
        _row_hash,
        json_extract_scalar(raw_json, '$.EmbryoID') as embryo_id,
        json_extract_scalar(raw_json, '$.PatientIDx') as patient_id_x,
        json_extract_scalar(raw_json, '$.TreatmentName') as treatment_name,
        json_extract_scalar(raw_json, '$.Evaluation.Evaluation') as kid_score,
        time_t2,
        time_t3,
        time_t4,
        time_t5,
        time_t8,
        time_t_blast_expand_last,
        value_morphological_grade,
        ROW_NUMBER() OVER (
            PARTITION BY json_extract_scalar(raw_json, '$.EmbryoID') 
            ORDER BY _extraction_timestamp DESC
        ) as rn
    FROM bronze_embryoscope_staging.embryo_data
)
SELECT 
    *
FROM deduplicated_embryos
WHERE rn = 1
  -- Cascading Filter: Only keep embryos associated with valid patients
  AND patient_id_x IN (SELECT patient_id_x FROM silver_embryoscope_staging.patients_filtered)
  
  -- Empty Annotations Filter: Keep only embryos with at least one non-null data point
  AND COALESCE(
      time_t2, time_t3, time_t4, time_t5, time_t8, 
      time_t_blast_expand_last, value_morphological_grade, kid_score
  ) IS NOT NULL;
```
